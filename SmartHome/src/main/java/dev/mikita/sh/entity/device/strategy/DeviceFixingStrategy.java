package dev.mikita.sh.entity.device.strategy;

import dev.mikita.sh.entity.device.ADevice;

public class DeviceFixingStrategy extends ADeviceStrategy {
    public DeviceFixingStrategy(ADevice device) {
        super(device);
    }

    @Override
    public void update() {

    }
}

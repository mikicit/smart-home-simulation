package dev.mikita.sh.entity.device.strategy;

import dev.mikita.sh.entity.device.ADevice;

public class DeviceIdleStrategy extends ADeviceStrategy {
    public DeviceIdleStrategy(ADevice device) {
        super(device);
    }

    @Override
    public void update() {

    }
}

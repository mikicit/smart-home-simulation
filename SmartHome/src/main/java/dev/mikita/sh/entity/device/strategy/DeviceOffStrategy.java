package dev.mikita.sh.entity.device.strategy;

import dev.mikita.sh.entity.device.ADevice;

public class DeviceOffStrategy extends ADeviceStrategy {
    public DeviceOffStrategy(ADevice device) {
        super(device);
    }

    @Override
    public void update() {

    }
}

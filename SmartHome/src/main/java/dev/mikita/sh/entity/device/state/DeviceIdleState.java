package dev.mikita.sh.entity.device.state;

import dev.mikita.sh.entity.device.ADevice;

public class DeviceIdleState extends ADeviceState {
    public DeviceIdleState(ADevice device) {
        super(device);
    }
}

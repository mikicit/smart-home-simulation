package dev.mikita.sh.entity.device.state;

import dev.mikita.sh.entity.device.ADevice;

public class DeviceOffState extends ADeviceState {
    public DeviceOffState(ADevice device) {
        super(device);
    }
}

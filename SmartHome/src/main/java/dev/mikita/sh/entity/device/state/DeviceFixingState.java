package dev.mikita.sh.entity.device.state;

import dev.mikita.sh.entity.device.ADevice;

public class DeviceFixingState extends ADeviceState {
    public DeviceFixingState(ADevice device) {
        super(device);
    }
}
